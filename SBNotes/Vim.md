```vim
:%! date "" Replace the whole buffer with output from command

:%! xxd "" Or this can be taken even further use the current buffer as input to command

:r! date "" Insert whole buffer with output from command
```
