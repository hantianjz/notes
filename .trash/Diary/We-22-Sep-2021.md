# TODO
- The WHD driver porting is really hard
    - GPIO control
    - Malloc replacing
    - Semaphore mapped to events
    - Maybe: Irq handler
    - And GPIO control also pulls in the system critical section control, this may lead to other problems.
    - The WHD library also uses a buffer management interface, why then in hell does it still use malloc?
    - And also upper level network interface callbacks, but that is much minor thing
    - Going to try modify WHD in abstract and see if we can make better more concrete progress this way, this will take longer, but less insane?
    - Maybe LwIP will be very hard to use without threads OS, but still possible?
    - Need to understand how the HTTP + TLS layer work together